import { SendButton } from "./SendButton";
import { Message } from "./Message";

export function SendMess() {
    return (
        <div>
            <Message />
            <SendButton />
        </div>
    );
}